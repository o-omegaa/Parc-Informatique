spring:
  datasource:
    url: jdbc:mysql://localhost:3306/supplier_portal?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true

  # ── SMTP Email (Gmail) ──────────────────────────────────────────
  mail:
    host: smtp.gmail.com
    port: 587
    username: bob77avg77@gmail.com
    password: cbvnlaphfkekniup
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com
        transport:
          protocol: smtp
    default-encoding: UTF-8

app:
  jwt:
    secret: SupplierPortalONEE2025SecretKeyDGSSICompliantXYZ123
    access-token-expiration-ms: 86400000
  bootstrap:
    admin-password: Admin@1234

logging:
  level:
    root: WARN
    com.supplierportal: INFO
    org.hibernate.SQL: DEBUG
